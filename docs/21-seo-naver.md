# 21. 네이버 SEO 구현 명세

- 최종 **가이드 확인일**: 2026-09-07
- 다음 확인 예정: 2026-12-07 (분기마다)

---

## 0. 이 문서 사용법 (중요)

[20-seo-google.md](./20-seo-google.md) §0과 같은 방식이다. **"문서 → 코드"** 방향.
네이버 가이드가 바뀌면: [출처]에서 확인 → 이 문서 §3 매핑표·해당 절 수정 → §8 이력 기록 → 코드 반영 → 서치어드바이저에서 점검.

### [출처] (확인은 항상 아래 공식 페이지에서)

| 주제 | 공식 URL |
|------|----------|
| 네이버 서치어드바이저 | https://searchadvisor.naver.com |
| 웹마스터 가이드(SEO 가이드) | https://searchadvisor.naver.com/guide |
| 서치어드바이저 공지사항 | https://searchadvisor.naver.com/notice |
| 네이버 검색 블로그(정책·알고리즘 발표) | https://blog.naver.com/naver_search |
| robots.txt / 사이트맵 도움말 | 서치어드바이저 > 도움말 |

> 네이버 공식 문서는 로그인/차단으로 외부에서 안 열릴 때가 있다. **서치어드바이저에 로그인해서** 직접 확인한다.

---

## 1. 네이버가 구글과 다른 점 (먼저 알아둘 것)

| 구분 | 구글 | 네이버 |
|------|------|--------|
| 새 사이트 발견 | 봇이 알아서 크롤링 | **자동으로 안 함.** 서치어드바이저에 등록·소유확인·사이트맵 제출·수집요청을 해야 색인 시작 |
| 크롤러 | Googlebot | **Yeti** (User-agent: `Yeti`) |
| 검색결과 구성 | 웹 문서 위주 | 통합검색(블로그·카페·지식iN·뷰·플레이스 등 자체 서비스 비중 큼). 다만 **웹사이트/웹문서 수집 영역이 확대되는 추세** |
| 랭킹 개념 | 페이지 단위 품질·링크·의도 | **적합도 + 인기도 + 신뢰도** 3축. 출처 단위 신뢰(C-Rank), 문서 단위 품질(D.I.A / D.I.A+) |
| 사이트맵 | 여러 개 자유 | **사이트당 1개 권장**, 여러 개면 각각 따로 제출 |
| 피드 | 선택 | **RSS/Atom 피드 제출** 권장 |

핵심: **네이버는 "등록해야 보인다."** 한국 사용자 검색의 상당 부분이 네이버이므로 등록은 필수.

---

## 2. 네이버 랭킹 요소 (개념)

정확한 공식은 비공개. 공개된 개념 수준에서:

- **적합도(relevance)**: 검색어와 문서 내용의 일치. title·본문·대표이미지·문서 구조가 주제를 분명히 드러내는가.
- **인기도(popularity)**: 클릭·체류·재검색 등 사용자 반응.
- **신뢰도(reliability)**: 출처의 일관성·전문성, 광고성/과장/낚시가 아닌가.
- **C-Rank**: 특정 주제에 대해 그 출처(사이트/블로그)가 **꾸준히 쌓아온 맥락과 신뢰**. → 한 주제(예: 생활 계산)를 꾸준히 다루는 것이 유리.
- **D.I.A / D.I.A+**: 문서 자체의 품질·정보성·**직접 경험/구체성**, 사용자가 선호하는 문서 패턴 학습.

계산기 사이트에 적용:
- 계산기별로 **직접 쓴 설명·예시·근거 출처**를 넣는다(빈 위젯 페이지 금지 — [12-page-structure.md](./12-page-structure.md) §5).
- "생활 계산기"라는 주제를 일관되게, 페이지를 꾸준히 늘린다.
- 과장·낚시 title 금지(신뢰도).

---

## 3. 구현 매핑표 (요구사항 → 코드 위치)

가이드가 바뀌면 "권장값"을 고치고 "구현 위치"를 수정한다.
공통 항목(title, description, canonical, 모바일, HTTPS, sitemap.xml, 404, 구조화 데이터 등)은
[20-seo-google.md](./20-seo-google.md) §3 매핑표를 그대로 따른다. **아래는 네이버 때문에 추가로 필요한 것**만.

| # | 항목 | 권장값 / 규칙 | 구현 위치 | 현재 상태 |
|---|------|----------------|-----------|-----------|
| N1 | 소유확인 메타태그 | `<meta name="naver-site-verification" content="{발급코드}">` (또는 HTML 파일 업로드 방식) | `Head.astro`(전역) 또는 `public/naver{코드}.html` | 미구현 |
| N2 | Open Graph 태그 | og:title, og:description, og:image, og:url — **네이버는 검색·공유 미리보기에서 OG를 적극 사용**. 대표 이미지 필수 | `Head.astro` + `public/images/og-default.png` | 미구현 |
| N3 | robots.txt에서 Yeti 허용 | `User-agent: Yeti` `Allow: /` (전체 허용 시 `User-agent: *`로 충분하나 명시 가능) + `Sitemap:` 절대경로 | `public/robots.txt` | 미구현 |
| N4 | 사이트맵 | **1개**로 유지(`sitemap.xml` 또는 `sitemap-index.xml`). 서치어드바이저에 제출 | `@astrojs/sitemap` (`astro.config.mjs`) | 미구현 |
| N5 | RSS/Atom 피드 | 신규·업데이트 페이지를 담은 피드. 서치어드바이저에 제출 | `@astrojs/rss` → `src/pages/rss.xml.ts` (콘텐츠 쌓이면 도입) | 나중 |
| N6 | `<link rel="alternate" type="application/rss+xml">` | RSS 도입 시 head에 추가 | `Head.astro` | 나중 |
| N7 | 모바일 대응 | 반응형(동일 URL). 서치어드바이저 "모바일 친화도"에서 통과 | `global.css` — [30-design-guide.md](./30-design-guide.md) | 미구현 |
| N8 | 웹 표준 | 유효한 HTML. 서치어드바이저 "웹 표준 검증" 통과 | 전역 마크업 품질 | 점검 필요 |
| N9 | 프로토콜/호스트 통일 | http→https, www↔non-www 중 하나로 301. **네이버는 이들을 별개 사이트로 취급** → canonical + 리다이렉트 일치 | Vercel 리다이렉트 설정 + `canonical`(`site.ts`) | 배포 시 |
| N10 | title/description | 낚시·과장 금지, 페이지 고유, 핵심 키워드 자연스럽게 | `Head.astro` (§20 §2 패턴 공유) | 미구현 |
| N11 | 대표 이미지 | 각 계산기 페이지에 의미 있는 대표 이미지(없으면 기본 OG). 너무 작지 않게 | 페이지 props / 기본값 | 미구현 |

---

## 4. 서치어드바이저 등록 절차 (배포 직후 1회)

1. **사이트 등록**: https://searchadvisor.naver.com → 웹마스터 도구 → 사이트 URL 입력.
   - `https://` + 최종 호스트(www 쓸지 말지 확정 후 그 형태로).
2. **소유 확인**: HTML 태그(권장) 또는 HTML 파일 업로드.
   - 태그 방식: 발급된 `<meta name="naver-site-verification" ...>`를 `Head.astro`에 넣고 배포 → "확인".
3. **robots.txt 확인**: 요청 > robots.txt → Yeti 차단 없는지, `Sitemap:` 줄 있는지.
4. **사이트맵 제출**: 요청 > 사이트맵 제출 → `https://{도메인}/sitemap-index.xml` (또는 `sitemap.xml`) 입력. **여러 개를 한 줄에 붙여 넣지 말 것.**
5. **RSS 제출**(피드 도입 후): 요청 > RSS 제출.
6. **웹페이지 수집 요청**: 요청 > 웹페이지 수집 → 홈·주요 계산기 URL 등록. **하루 약 50 URL 한도**이므로 여러 날에 나눠서.
7. **진단 도구 실행**: 검증 > 웹 표준 검증 / 모바일 친화도 / (지원되면) 리치리절트. 오류 정리.
8. 색인까지 보통 **1~4주**. 조급해하지 말 것.

> HTTP/HTTPS, www/non-www를 **각각** 등록해두면(리다이렉트는 하되) 어느 쪽으로 들어와도 데이터가 잡힌다.

---

## 5. 콘텐츠 권장 (네이버 관점)

- **원본성**: 다른 사이트 설명 복붙 금지. 공식 출처를 참고하되 문장은 직접 쓴다.
- **구체성·경험**: "이렇게 입력하면 이런 결과" 예시를 숫자로. 단순 공식 나열보다 낫다.
- **주제 일관성**: 생활 계산이라는 축을 유지하며 페이지를 꾸준히 추가(C-Rank).
- **신뢰 신호**: 근거(법령·공공기관) 링크, 최종 업데이트 날짜 표기, 운영자 소개(About), 개인정보처리방침.
- **낚시 금지**: title과 내용 불일치, 과장("무조건", "100% 정확") 표현 지양 → 신뢰도 하락.
- **광고 과다 금지**: 본문보다 광고가 먼저 보이는 배치 지양(사용자 반응 악화 → 인기도 하락). 광고 규칙은 나중에 `41-ads-adsense.md`.

---

## 6. 정기 점검 체크리스트 (분기 1회 + 네이버 공지 있을 때)

- [ ] [출처]의 서치어드바이저 공지사항 / 네이버 검색 블로그에서 지난 분기 변경 확인
- [ ] 서치어드바이저: 사이트 요약의 수집/색인 추이, 오류
- [ ] 웹 표준 검증 / 모바일 친화도 재실행
- [ ] 사이트맵·RSS 제출 상태 정상인지, 신규 페이지가 반영되는지
- [ ] 새 계산기 URL 수집 요청 (한도 내에서)
- [ ] 세율·요율 최신 여부([02-calculator-catalog.md](./02-calculator-catalog.md))
- [ ] 이 문서 §0 "다음 확인 예정" 날짜 갱신

---

## 7. 구글과 공유하는 부분 (중복 구현 안 함)

아래는 [20-seo-google.md](./20-seo-google.md)에서 이미 정의됨. 네이버도 동일하게 혜택:
`<html lang>`, utf-8/viewport, title/description, canonical, `sitemap.xml`, `robots.txt` 기본,
시맨틱 마크업/`h1` 1개, 이미지 `alt`, 내부 링크, HTTPS, 실제 404, 반응형, 성능(CWV).

→ **SEO 구현체는 하나.** 이 문서는 그 위에 "네이버라서 추가로 필요한 것"만 얹는다.

---

## 8. 변경 이력

| 날짜 | 확인한 것 / 바꾼 것 |
|------|---------------------|
| 2026-09-07 | 최초 작성. 서치어드바이저 등록 절차, Yeti, 사이트맵 1개 원칙, RSS 제출, 수집요청 일 50 URL, http/https·www 분리 취급, OG 태그 중요성, C-Rank/D.I.A 개념 반영 |
